input {
  kafka {
    bootstrap_servers => "127.0.0.1:9092"
    topics => ["logs"]
  }
}

filter {
  kv { }
  mutate {
    convert => { "time" => "integer" }
  }
}

output {
  elasticsearch {
    hosts => ["localhost:9200"]
  }
  stdout {
    codec => rubydebug
  }
}